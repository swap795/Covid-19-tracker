import React from 'react';
//import { MenuItem, FormControl, Select } from '@material-ui/core';

function Deaths(countries) {
   return(
      <div>
         {countries.deaths}
      </div>
   );
}

export default Deaths;